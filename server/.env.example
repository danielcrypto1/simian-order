PORT=4000
DATABASE_URL=postgres://simian:simian@localhost:5432/simian
FCFS_TOTAL_SPOTS=50
REFERRAL_LIMIT=5
ADMIN_KEY=dev-only-change-me
ADMIN_USER=admin
ADMIN_PASS=changeme
SESSION_SECRET=change-me-please
CORS_ORIGIN=http://localhost:3000

# 0x-prefixed 32-byte hex. NEVER commit. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MINT_SIGNER_PRIVATE_KEY=
